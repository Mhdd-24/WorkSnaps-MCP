# Copy to .env and fill in your values (do not commit .env)

WORKSNAPS_API_TOKEN=

# Optional defaults for log_time when not passed per call
WORKSNAPS_DEFAULT_PROJECT_ID=
WORKSNAPS_DEFAULT_TASK_ID=

# Optional override (default: https://api.worksnaps.com/api)
# WORKSNAPS_BASE_URL=https://api.worksnaps.com/api
